#include "main.h"

/**
 *print_last_digit - description
 *@c: para
 *Return: 0
 */

int print_last_digit(int c)

{
	if (c < 0)
	{
		n = (n % 10);
	}
	else
	{
		n = (n % 10);
	}
	_putchar(n + '0');
	return (n);
}
