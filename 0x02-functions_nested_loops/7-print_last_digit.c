#include "main.h"

/**
 *print_last_digit - description
 *@c: para
 *Return: 0
 */

int print_last_digit(int c)

{
	if (c < 0)
	{
		c = (n % 10);
	}
	else
	{
		c = (n % 10);
	}
	_putchar(c + '0');
	return (c);
}
